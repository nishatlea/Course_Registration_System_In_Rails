<%= form_with(model: semester_curriculum, local: true) do |form| %>
  <% if semester_curriculum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(semester_curriculum.errors.count, "error") %> prohibited this semester_curriculum from being saved:</h2>

      <ul>
        <% semester_curriculum.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :semester_id %>
    <%= form.text_field :semester_id %>
  </div>

  <div class="field">
    <%= form.label :curriculum_id %>
    <%= form.text_field :curriculum_id %>
  </div>

  <div class="field">
    <%= form.label :references %>
    <%= form.text_field :references %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.text_field :grade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
